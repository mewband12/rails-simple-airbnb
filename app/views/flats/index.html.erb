<html>
<!-- Image and text -->
<nav class="navbar navbar-light bg-light">
  <%= link_to "Home",  flats_path, class: "navbar-brand"%>
  <%= link_to "create tasks",  new_flat_path , class:"mr-sm-2 navbar-brand"%>
</nav>
<div class = "d-block ml-100 d-flex justify-content-center">
    <div class ="col-sm-8 col-sm-offset-2" style="">
        <h1>My Flats</h1>
          <% @flats.each do |flat| %>
          <div class = "card">
            <h5 class="card-header navbar-brand"><%=flat.name%></h5>
            <div class ="card-body">
              <%= flat.address %>
              <p><%= link_to "more info",  flat_path(flat.id) %></p>
              <p>
                <%= link_to "🗑", flat_path(flat.id), method: :delete, data: { confirm: "Really delete #{flat.name}?" } %>
              </p>
              <p><%= link_to "edit", edit_flat_path(flat.id)  %></p>
            </div>
          </div>
              <% end %>
        <p><%= link_to "create tasks",  new_flat_path %></p>
    </div>
</div>



</html>
